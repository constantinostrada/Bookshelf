# ─────────────────────────────────────────────
# Bookshelf — environment variable template
# Copy to .env.local and fill in real values.
# NEVER commit .env.local to version control.
# ─────────────────────────────────────────────

# Application
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Persistence (future use — swap in-memory repo for a real DB)
# DATABASE_URL=postgres://user:password@localhost:5432/bookshelf
